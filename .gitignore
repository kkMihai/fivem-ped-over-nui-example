# deps
node_modules/
.pnp
.pnp.js

# vite build output (ship it or build it yourself, see README)
web/build/
web/dist/
*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env
.env
.env.*
!.env.example

# editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# os
.DS_Store
Thumbs.db
desktop.ini

# fivem
cache/
*.cfg.bak

# ignoring lock files - not recommended but i do it because some people use npm, some use yarn, some use bun, and so to avoid a small problem
package-lock.json
npm-shrinkwrap.json
yarn.lock
pnpm-lock.yaml
bun.lock
bun.lockb